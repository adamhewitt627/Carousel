using Windows.UI.Xaml.Controls;

namespace KF.CarouselRepeater
{
    public sealed class Carousel : Control
    {
        public Carousel()
        {
            DefaultStyleKey = typeof(Carousel);
        }
    }
}
